# Python
.venv/
env/
__pycache__/
**/__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/

# Environment
.env

# Local file storage (uploads) — anchored to the backend root only, so this
# does NOT also match backend/modules/storage/ (that bug is what deleted the
# storage module from git history in the first place).
/storage/
